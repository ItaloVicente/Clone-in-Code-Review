    /**
     * @param window
     * @param side
     */
    public EditorAreaDropTarget(IWorkbenchWindowProvider provider, int side) {
        super(provider);
        this.side = side;
    }
