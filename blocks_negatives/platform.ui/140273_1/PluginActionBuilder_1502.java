                    WorkbenchPlugin
									+ menuElement.getContributor().getName()
                    return;
                }
            }

            IMenuManager newMenu = parent.findMenuUsingPath(id);
            if (newMenu == null) {
