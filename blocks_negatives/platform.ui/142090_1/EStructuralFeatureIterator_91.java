    /**
     * Computes length for the current pointer - ignores any exceptions.
     * @return length
     */
    private int getLength() {
        int length;
        try {
        }
        catch (Throwable t) {
            length = 0;
        }
        return length;
    }
