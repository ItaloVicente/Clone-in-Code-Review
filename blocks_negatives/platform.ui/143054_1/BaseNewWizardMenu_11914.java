            for (Object object : objects) {
                if (object instanceof NewWizardShortcutAction) {
                    actions.values().remove(object);
                }
            }
        }
