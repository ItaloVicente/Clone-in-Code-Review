    if (stale == Stale.OK) {
      args.put(STALE, stale);
    } else if (stale == Stale.UPDATE_AFTER) {
      args.put(STALE, stale);
    } else if (stale == Stale.FALSE) {
      args.put(STALE, stale);
    }
