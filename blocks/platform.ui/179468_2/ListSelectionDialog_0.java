		this(parentShell, input, contentProvider, labelProvider, message, false, false);
	}

	public ListSelectionDialog(Shell parentShell, Object input, IStructuredContentProvider contentProvider,
			ILabelProvider labelProvider, String message, boolean useShortButtonLabels,
			boolean updateButtonsEnablement) {
