	    switch (vert) {
	    	case PreferenceConstants.PREF_DECORATOR_VERTICALLOCATION_BOTTOM:
		    bits = SWT.BOTTOM;
		    break;
	    	case PreferenceConstants.PREF_DECORATOR_VERTICALLOCATION_CENTER:
		    bits = SWT.CENTER;
		    break;
	    	default:
		    bits = SWT.TOP;
		    break;
	    }
