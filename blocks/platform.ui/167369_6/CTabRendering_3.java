		int margin = shadowEnabled ? SIDE_DROP_WIDTH
				: 0 + countPixelsToDrawDistrinctColors(outerKeylineColor, innerKeylineColor);
		points[index++] = margin;
		points[index++] = bottomY;
		points[index++] = startX;
		points[index++] = bottomY;
