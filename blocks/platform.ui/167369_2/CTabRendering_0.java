		points[index++] = parentSize.x - 1 - margin;
		points[index++] = parentSize.y - margin;

		points[index++] = points[0];
		points[index++] = parentSize.y - margin;

		points[index++] = points[0];
		points[index++] = points[1];

