			if (selection != null) {
				c.addVariable(ISources.ACTIVE_CURRENT_SELECTION_NAME, selection);
			}
			if (showUpdateReferences) {
				c.addVariable("showUpdateReferences", showUpdateReferences); // TODO: Move string constant //$NON-NLS-1$
			}
