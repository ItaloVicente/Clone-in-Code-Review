				if (!allowAdd) {
					setErrorMessage(
							UIText.GitSelectRepositoryPage_NoRepository);
				} else {
					setErrorMessage(
							UIText.GitSelectRepositoryPage_NoRepoFoundMessage);
				}
