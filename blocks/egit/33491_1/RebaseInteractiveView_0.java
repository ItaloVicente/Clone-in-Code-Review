						if (RebaseInteractivePreferences.isOrderReversed())
							return UIIcons.getImage(resources,
									UIIcons.FIXUP_REVERSED);
						else
							return UIIcons.getImage(resources,
									UIIcons.FIXUP_DIRECT);
