		assertEquals(name
				read.getPathString()
				"Paths of length " + name.length() + " should match");
		assertEquals(-1
		assertArrayEquals(md0.digest()
				md.digest()
				"Digests should match");
