	
	static Comparator<DfsPackDescription> commitGraphComparator() {
		return (a
			int c = PackSource.DEFAULT_COMPARATOR.reversed().compare(a.getPackSource()
			if (c != 0) {
				return c;
			}
			return Long.signum(b.getLastModified() - a.getLastModified());
		};
	}
	
