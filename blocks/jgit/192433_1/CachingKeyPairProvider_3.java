			PublicKey pk = result.getPublic();
			if (pk != null) {
				Map<String
						.getAttribute(KEY_PATHS_BY_FINGERPRINT);
				if (registered == null) {
					registered = new HashMap<>();
					session.setAttribute(KEY_PATHS_BY_FINGERPRINT
				}
				registered.put(KeyUtils.getFingerPrint(pk)
			}
