				if (dce.getFileMode() == FileMode.GITLINK) {
					remove(name);
				} else {
					if (hId.equals(iId)) {
						if (f == null || f.isModified(dce
							conflict(name
						else
							remove(name);
					} else
