			long timeLimit = timeout.toMillis();
			long remainingTime[] = { timeLimit };
			session = connect(uri
					future -> notifyCloseListeners()
					timeLimit > 0
		} catch (IOException e) {
			disconnect(e);
			throw e;
		}
	}
