						JGitText.get().collisionOn
			}
			pck = inflate(Source.DATABASE
			while (0 < sz) {
				int n = (int) Math.min(readBuffer.length
				IO.readFully(cur
				IO.readFully(pck
				for (int i = 0; i < n; i++) {
					if (curBuffer[i] != readBuffer[i]) {
						throw new IOException(MessageFormat.format(JGitText
								.get().collisionOn
