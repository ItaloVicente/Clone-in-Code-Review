			int e = parents.length - 1;
			for (int i = 0; i < e; i++) {
				RevCommit p = parents[i];
				if (carryOntoOne(p
					stack = new CarryStack(stack
				}
			}

			c = parents[e];
			if (carryOntoOne(c
