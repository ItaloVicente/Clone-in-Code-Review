		try (TreeWalk walk = beginWalk()) {
			assertIteration(walk
			assertIteration(walk
			assertIteration(walk
			assertIteration(walk
			assertIteration(walk
			assertIteration(walk
			assertIteration(walk
			assertIteration(walk
			assertIteration(walk
			assertFalse("Not all files tested"
		}
