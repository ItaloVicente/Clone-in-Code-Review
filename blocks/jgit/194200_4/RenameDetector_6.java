	private void findRenames(ContentSource.Pair reader
			List<DiffEntry> addDiffs
			throws CanceledException
		if (!addDiffs.isEmpty() && !deleteDiffs.isEmpty())
			findExactRenames(pm

		if (!addDiffs.isEmpty() && !deleteDiffs.isEmpty())
			findContentRenames(reader
	}

