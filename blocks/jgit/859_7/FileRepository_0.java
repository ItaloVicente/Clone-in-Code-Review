		if (objectDir != null) {
			objectDatabase = new ObjectDirectory(repoConfig
					fs.resolve(objectDir
					alternateObjectDir
					fs);
		} else {
			objectDatabase = new ObjectDirectory(repoConfig
					fs.resolve(gitDir
					alternateObjectDir
					fs);
		}
