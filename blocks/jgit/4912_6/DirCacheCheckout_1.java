					final int mMode = m.getEntryRawMode();
							&& f.isModified(dce
						conflict(name
						return;
					}

					final int iMode = i.getEntryRawMode();
							&& (iMode & FileMode.TYPE_FILE) != 0) {
						update(name
						return;
					}

