						if (LOG.isDebugEnabled()) {
							LOG.debug(String.format(
									"Opening pack index %s
									extFile(INDEX).getAbsolutePath()
									Float.valueOf(extFile(INDEX).length()
											/ (1024f * 1024))
									Long.valueOf(System.currentTimeMillis()
											- start)));
						}
