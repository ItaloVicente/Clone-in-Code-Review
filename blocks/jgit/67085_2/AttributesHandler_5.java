		int i = cachePath.lastIndexOf('/');
		String parentCachePath = (i >= 0 ? cachePath.substring(0
				: ROOT_PATH);
		CacheEntry parentEntry = mergeAndCacheRules(parentCachePath
				parentOf(workingTreeIterator)
				parentOf(otherTree));

		entry = new CacheEntry(parentEntry
				treeWalk

		entryPerDir.put(entry.path
		return entry;
