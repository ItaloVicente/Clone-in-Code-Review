			if (primary != null) {
				if (!primary.equals(this) && !alt.in(primary.myAlternates())) {
					primary.addAlternate(alt);
					if (alt.db.hasObjectImpl1(objectId
						return true;
				}
			} else if (alt.db.hasObjectImpl1(objectId
