		if (objectDir != null) {
			objectDatabase = new ObjectDirectory(config
					fs.resolve(objectDir
					alternateObjectDir
					fs);
		} else {
			objectDatabase = new ObjectDirectory(config
					fs.resolve(gitDir
					alternateObjectDir
					fs);
		}
