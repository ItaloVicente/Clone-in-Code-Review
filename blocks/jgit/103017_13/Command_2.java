	static Ref toRef(RevWalk rw
			String name
			throws MissingObjectException
		if (target != null) {
			return new SymbolicRef(name
					new ObjectIdRef.Unpeeled(NETWORK
		} else if (ObjectId.zeroId().equals(id)) {
