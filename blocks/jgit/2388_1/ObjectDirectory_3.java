	private static class CachedPackList {
		final FileSnapshot snapshot;

		final Collection<LocalCachedPack> packs;

		final byte[] raw;

		CachedPackList(FileSnapshot sn
			snapshot = sn;
			packs = list;
			raw = buf;
		}
	}

