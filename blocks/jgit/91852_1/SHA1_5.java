		save(h
	}

	@SuppressWarnings("fallthrough")
	private void recompress(int t
		int[] s;
		if (t == 58) {
			s = state58;
		} else if (t == 65) {
			s = state65;
		} else {
			throw new IllegalStateException();
