	@Override
	protected Map<KexProposalOption
			Map<KexProposalOption
		Map<KexProposalOption
				guess);
		if (log.isDebugEnabled()) {
			result.forEach((option
					"setNegotiationResult({}) Kex: {} = {}"
					option.getDescription()
		}
		return result;
	}

