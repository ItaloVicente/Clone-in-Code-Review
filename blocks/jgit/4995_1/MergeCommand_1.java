				String msg = null;
				ObjectId newHead
				if (!squash) {
					updateHead(refLogMessage
					newHead = base = srcCommit;
				} else {
					msg = JGitText.get().squashCommitNotUpdatingHEAD;
					newHead = base = headId;
				}
