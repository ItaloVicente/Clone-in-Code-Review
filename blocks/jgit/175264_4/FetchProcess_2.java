			Map<String
			if (isInitialBranchMissing(refsMap
				throw new TransportException(MessageFormat.format(
						JGitText.get().remoteBranchNotFound
			}
			result.setAdvertisedRefs(transport.getURI()
