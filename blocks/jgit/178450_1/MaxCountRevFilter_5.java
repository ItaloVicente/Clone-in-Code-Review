		}
		if (and == null || and.include(walker
			if (++count >= maxCount) {
				stop = true;
			}
			return true;
		} else {
			return false;
		}
	}

	@Override
	public boolean requiresCommitBody() {
		return requiresCommitBody;
