			if (!inCore) {
				if (work != null
						&& (!nonTree(work.getEntryRawMode()) || work
								.isModified(index.getDirCacheEntry()
										true
					failingPathes.put(tw.getPathString()
							MergeFailureReason.DIRTY_WORKTREE);
					return false;
				}
