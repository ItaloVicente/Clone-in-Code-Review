		if (searchPath(path
			String w = readPipe(userHome()
					new String[] { "bash"
					Charset.defaultCharset().name());
			if (w != null) {
				gitExe = resolve(null
				if (gitExe != null)
					return resolveGrandparentFile(gitExe);
			}
