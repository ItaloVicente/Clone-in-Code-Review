
		if (gitExe == null) {
			if (searchPath(path
				String w = readPipe(userHome()
						new String[]{"bash"
						Charset.defaultCharset().name());
				if (!StringUtils.isEmptyOrNull(w))
					gitExe = resolve(null
			}
