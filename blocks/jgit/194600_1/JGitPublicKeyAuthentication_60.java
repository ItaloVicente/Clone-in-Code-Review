					while (next == null && iter.hasNext()) {
						Map.Entry<PublicKey
						PublicKey pk = val.getKey();
						if (identityFiles == null || identityFiles.stream()
								.anyMatch(k -> KeyUtils.compareKeys(k
							next = val;
