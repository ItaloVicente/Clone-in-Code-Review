					Config directParent;
					if (additionalFile != null) {
						directParent = new FileBasedConfig(parent
								additionalFile
					} else {
						directParent = parent;
					}
					return new FileBasedConfig(directParent
				} else if (additionalFile != null) {
					return new FileBasedConfig(parent
							FS.DETECTED);
