			if (o.has(inCachedPack)) {
				CachedPack pack = tipToPack.get(o);
				if (includesAllTips(pack
					useCachedPack(walker
							wantObjs

					countingMonitor.endTask();
					countingMonitor.beginTask(JGitText.get().countingObjects
							ProgressMonitor.UNKNOWN);
					continue;
				}
			}

