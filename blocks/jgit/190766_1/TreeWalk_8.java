		if (oldLen == newLen) {
			Arrays.fill(attrs
			Arrays.fill(attributesHandlers
		} else {
			attrs = new Attributes[newLen];
			attributesHandlers = new AttributesHandler[newLen];
		}
		headIndex = -1;
