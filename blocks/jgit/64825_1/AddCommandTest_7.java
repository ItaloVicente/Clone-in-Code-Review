		try (Git git = new Git(db)) {
			db.getConfig().setString("core"
			git.add().addFilepattern("a.txt").call();
			assertEquals("[a.txt
					indexState(CONTENT));
			db.getConfig().setString("core"
			git.add().addFilepattern("a.txt").call();
			assertEquals("[a.txt
					indexState(CONTENT));
			db.getConfig().setString("core"
			git.add().addFilepattern("a.txt").call();
			assertEquals("[a.txt
					indexState(CONTENT));
		}
