        this(prefix
	}

    protected WorkingTreeIterator(final String prefix
        this(prefix
    }

    WorkingTreeIterator(final String prefix
		super(prefix
		nameEncoder = pathNameEncoding.newEncoder();
