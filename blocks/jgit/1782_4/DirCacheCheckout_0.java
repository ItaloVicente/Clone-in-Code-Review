			if (i == null || f == null || !m.idEqual(i)
					|| f.isModified(i.getDirCacheEntry()
							config_filemode()
				update(m.getEntryPathString()
						m.getEntryFileMode());
			} else
				keep(i.getDirCacheEntry());
