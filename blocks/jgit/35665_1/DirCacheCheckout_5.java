	public static void checkoutEntry(final Repository repository
			DirCacheEntry entry) throws IOException {
		checkoutEntry(repository
	}

	public static void checkoutEntry(final Repository repo
			DirCacheEntry entry
		checkoutEntry(repo
	}

