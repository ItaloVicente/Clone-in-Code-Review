			if (isWildcard(s)) {
				if (mode == wildcardmode.WILD_CARD_MATCH) {
					throw new IllegalArgumentException(MessageFormat
							.format(JGitText.get().invalidWildcards
				}
				wildcard = true;
			}
