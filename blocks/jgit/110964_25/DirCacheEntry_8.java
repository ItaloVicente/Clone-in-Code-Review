	private void setExtendedFlags(int flags) {
		if (isExtended()) {
			NB.encodeInt16(info
		} else {
			String message = MessageFormat.format(
					JGitText.get().indexEntryNotExtendable
			throw new UnsupportedOperationException(message);
		}
	}

